<%= form_for [chatroom, message], data: {action: 'turbo:submit-end->messages#clearForm'} do |f| %>
  <%= f.text_field :text %>
  <%= f.submit  'Send', class: 'bg-emerald-500 hover:bg-emerald-600 text-white font-semibold py-2 px-4 rounded cursor-pointer'%>
<% end %>
