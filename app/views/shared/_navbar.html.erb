<% if current_user %>
  <div class="flex w-full justify-between items-center px-8">
    <p>👤 <%= current_user.display_name %></p>
    <%= link_to 'Log out', destroy_user_session_path, data: {turbo_method: :delete}, class: 'px-4 py-2 bg-primary hover:bg-primary-dark cursor-pointer text-white rounded' %>
  </div>
<% end %>
